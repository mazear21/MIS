# MIS System Environment Configuration
# Copy this file to .env and update the values

# Flask Secret Key (change this in production!)
SECRET_KEY=mis-system-secret-key-change-in-production

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mis_system
DB_USER=postgres
DB_PASSWORD=your_password_here
